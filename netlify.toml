[build]
  command = "npm run build"
  publish = "dist"
  node_version = "20"

# SPA fallback: serve index.html for all client-side routes
# so deep links like /brotherhood/challenges don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
